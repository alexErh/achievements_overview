export class Module {

    public code: string;
    public name: string;
    public crp: number;

    constructor(code: string, name: string, crp: number) {
        this.code = code;
        this.name = name;
        this.crp = crp;
    }
}
